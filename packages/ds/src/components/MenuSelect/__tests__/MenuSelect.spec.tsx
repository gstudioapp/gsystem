import '@testing-library/jest-dom';

import { setup } from '../../../test-utils';
import { MenuSelectProps } from '../MenuSelect';

const initalProps: MenuSelectProps = {};

describe('MenuSelect', () => {
  it('should do something', () => {
    // TODO: Create MenuSelect test cases
  });
});
