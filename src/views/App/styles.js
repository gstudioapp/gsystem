import tokens from '../../temp/tokens-temp.json'

export default (props) => {
    return `
        background-color : ${tokens.colorBrandPrimaryMedium};
        color: ${tokens.colorNeutralWhite};
    `
}